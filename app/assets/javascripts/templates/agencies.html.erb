<div class="container">
  <div class="col-md-12">
    <h1>Backend Challenge</h1>
  </div>
  <div class="col-md-8">
    <div ng-repeat="agency in agencies track by agency.id | orderBy:'grade' | filter:{name:nameFilter} | tagsFilter:tagFilter">
      <div class="panel panel-default">
        <div class="panel-heading">
          <div class="row">
            <div class="col-md-4">
              <h3 class="panel-title pull-left">{{agency.name}}</h3>
            </div>
            <div class="col-md-6">
              <ul class="panel-tags list-inline">
                <li class="panel-tag" ng-repeat="tag in agency.tag_list">{{tag}}</li>
              </ul>
            </div>
            <div class="col-md-2">
              <span class="panel-grade pull-right grade <%= '{{agency.grade}}' %>">{{agency.grade}}</span>
            </div>
          </div>
        </div>
        <div class="panel-body">
          <p>{{agency.description}}</p>
        </div>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <span class="panel-title">Filter your agencies</span>
      </div>
      <div class="panel-body">
        <form role="form">
          <div class="form-group">
            <label for="name">Filter by name:</label>
            <input id="name" type="text" class="form-control" placeholder="A1 Agency" ng-model="nameFilter">
          </div>
          <div class="form-group">
            <label for="tags">Filter by tags:</label>
            <input id="tags" type="text" class="form-control" placeholder="Digital Advertising, User Experience" ng-model="tagFilter">
          </div>
        </form>
      </div>
    </div>
    <div class="panel panel-default">
      <div class="panel-heading">
        <span class="panel-title">Add an agency</span>
      </div>
      <div class="panel-body">
        <form name="newAgency" role="form" ng-submit="submitForm()">
          <div class="form-group">
            <label for="newAgency.name">Name:</label>
            <input id="newAgency.name" type="text" class="form-control" ng-model="newAgency.name">
          </div>
          <div class="form-group">
            <label for="newAgency.description">Description:</label>
            <input id="newAgency.description" type="text" class="form-control" ng-model="newAgency.description">
          </div>
          <div class="form-group">
            <label for="newAgency.tag_list">Tags:</label>
            <input id="newAgency.tag_list" type="text" class="form-control" placeholder="separate with a comma" ng-model="newAgency.tag_list">
          </div>
          <div class="form-group">
            <label for="newAgency.grade">Grade:</label>
            <select id="newAgency.grade" name="newAgency.grade" type="text" class="form-control" ng-model="newAgency.grade">
              <option value="padawan">Padawan</option>
              <option value="jedi" selected>Jedi</option>
              <option value="master">Master</option>
            </select>
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>
